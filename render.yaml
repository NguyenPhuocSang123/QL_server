services:
  - type: web
    name: ql-server
    runtime: node
    region: singapore
    plan: free
    buildCommand: node build.js
    startCommand: npm start --prefix backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
